%h1= @article.title
%p= @article.content
%p= @article.url


= link_to 'Back', root_path
= link_to 'Edit', edit_article_path(@article)
= link_to 'Delete', article_path(@article), method: :delete, data: {confirm: "Are you sure? No Going back after this."}
